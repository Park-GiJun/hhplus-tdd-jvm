<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%white(%d{HH:mm:ss.SSS}) %magenta(||) %highlight(%-5level) %magenta(||) %cyan(%logger{36}) %magenta(||) %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="io.hhplus.tdd" level="DEBUG" />

    <logger name="org.springframework" level="WARN" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>